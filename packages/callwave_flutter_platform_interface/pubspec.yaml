name: callwave_flutter_platform_interface
description: Platform interface for callwave_flutter. Defines the contract, DTOs, and wire serialization for the federated VoIP call UX plugin. Used by method channel and other platform implementations.
version: 0.1.0
homepage: https://github.com/Effdelorg/callwave_flutter
repository: https://github.com/Effdelorg/callwave_flutter
issue_tracker: https://github.com/Effdelorg/callwave_flutter/issues
license: BSD-3-Clause
resolution: workspace

environment:
  sdk: '>=3.5.0 <4.0.0'
  flutter: '>=3.22.0'

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.1.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
