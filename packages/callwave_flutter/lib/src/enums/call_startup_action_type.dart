/// Type of startup action from a missed-call notification launch.
enum CallStartupActionType {
  /// User tapped the notification body.
  openMissedCall,

  /// User tapped "Call Back".
  callback;
}
